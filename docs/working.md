# Working Log

项目工作记录。格式遵循 project_scaffold skill：Changelog（按日期）+ Lessons Learned（避坑）。
深度物理方法见 `PHYSICS_AND_PITFALLS.md`，折射几何见 `L1_geometry.md`。

## Key Technical Decisions（关键技术决策）

- **权威物理管线（2026-06-01 起）**：raytrace_eclipse.py 真·正向 ray tracing——真折射 RK4
  （refraction_trace，撞地遮挡涌现）+ 弯曲路径消光（curved_path，**用真实切点高度 z_tan**，
  2026-06-09 修正过 impact-h 混用 bug）+ 撒线 focusing/散焦涌现 + 半影直射光，零解析处方。
  视频/ablation step6/光度曲线都走它。下述解析链（L1 映射、render_rt 反查）保留用于
  快速渲染、直觉与 ablation 对照。
- **物理基础**：单次散射 + 消光-only 的 limb transmission。瑞利当消光、臭氧当吸收，
  Beer-Lambert `T=exp(-(N_air·σ_ray + N_O3·σ_o3))`。数据全实测：臭氧 Serdyuchenko 2014、
  大气 AFGL US Standard、太阳谱 SAO2010、CIE CMF。多次散射/气溶胶证实对绿松石带颜色无关键影响，未加。
- **折射几何**：单一映射 `r(h)=(R⊕+h)−α(h)·d_moon`，α(h)=0.0204·exp(-h/8)。对照 Mallama
  arXiv:2112.08966 Table 3.1 吻合<2%。低 h→本影深处(红核)、高 h→边缘(青绿)。
- **渲染架构**：逐像素反向 ray tracing（render_rt.py，分支感知反查），非 LUT 查表——
  根治 banding（banding 根因是非单调映射 argsort+角距轴插值的拓扑错误，非分辨率）。
  numpy 亚秒级，不需 MPS。
- **写实**：月面反照率纹理(NASA CGI Moon Kit) × 物理食光颜色，线性空间相乘，正交投影。
- **颜色科学**：固定白点(未衰减日光 Y=1)全程复用；色相判据用 sRGB 通道关系而非 CIELAB hue 角。
- **tone map**：对数高光肩部(白不死白)；曝光 expose_mode="faithful"(按最亮处标定,保留亮度悬崖,
  蓝带物理真实地窄) 为默认，"darks"(提暗部,蓝带显宽)仅作对比。dyn_gamma=1.0 不压动态范围。
- **HDR**：复用 YabiVision 的 Apple ImageIO gain map 写入(tools/make_gainmap_hdr.swift)，
  gain 从真实 HDR/SDR 比值精确算，输出可下 iPhone Photos 的 HEIC。
- **诊断工具**：diag_composite.py(剖面曲线量化蓝带宽度)、diag_brightness_cliff.py(亮度悬崖对比)、
  diag_moon_positions.py(不同月相位置)、diag_spectrum/geometry.py。

## Changelog

### 2026-05-31

- Scaffold 项目：git repo、uv venv (Python 3.12)、PRD/RFC/README、中文工作语言约定。
- Science review：多 agent 文献调研 + 数据侦察，确定物理模型与权威数据源。
- 下载权威数据：臭氧 Serdyuchenko 2014 截面、AFGL US Standard 大气廓线、CIE 1931 CMF。
- 实现 L0 辐射传输管线六模块（大气/截面/几何/太阳谱/RT/颜色），各自自查通过。
- L0 v1 色相曲线闭环全过：红区2-14km → 绿松石带16-36km → 白边，亮度暴跌240×。
- 修正颜色 per-height 归一化错误：k 改从未衰减日光算一次全程复用。
- 实现 L1 折射几何：α(h) + 本影内角距映射，对照 Robinson 2022 Table 3.1 吻合<2%。
- 主图横轴从擦边高度改为月面角度（双轴 arcmin + r/R_umbra）。
- 实现聚焦因子，按环带能量守恒 B∝b·|dh/dr|/r（修正初版方向反的错误）。
- 修正几何 bug：绿松石带改用对侧-limb 公式，从本影外 50-57' 归位到 34-41' 紧贴边界。
- 偏蓝诊断：确认 sRGB 红蓝比 0.88 落在文献 0.8-1.0 区间，是 CIELAB hue 度量误导，未改物理。
- 月盘 2D 渲染 render.py：复用辐射传输 LUT，月盘偏心贴图，全盘统一曝光，验收通过。
- 补写 PHYSICS_AND_PITFALLS.md（物理方法 + 5个坑全记录）、working.md。
- 真实几何月盘渲染：d=26'(食分0.99)让绿松石带横切月盘，红核/绿松石/白同盘可见。
- 叠加真实月面反照率纹理（Clementine 灰度图）× 物理食光颜色，正交投影，做出写实月食照片。
- **架构升级：逐像素反向 ray tracing（render_rt.py），替换 LUT 查表，根治 banding。**
  根因不是采样密度，是把非单调 角距(h) 映射 argsort 压平+角距轴插值的拓扑错误。
  解法：密 h 网格(8000点)算两 limb 的 h→(角距,XYZ,聚焦)，按角距极小值切单调分支，
  每像素在单调 a→h 反查、光滑 h→颜色取色、两 limb 辐照叠加。相邻色跳 0.0044(无台阶)，
  numpy 1.6s 不需 MPS。还修了旧 LUT 只用对侧 limb、红核暖靠透射谱凑的缺陷。
- render_textured 着色接入反向 RT：最终写实月食照片无 banding，颜色逐像素物理算出。
- 升级月面纹理为 NASA CGI Moon Kit (SVS #4720)：LROC 彩色 4k + LOLA 位移图。
- 影调调研（真实月食实拍）：渲染"假"的根因是把物理 5-8 档边-核梯度原样铺开。
  摄影成品压到视觉 1.5-2.5 档(3:1~5:1)。修法：tone-map 前对物理亮度做幂律压缩
  (dyn_gamma=0.42) 收窄动态范围，色相不动；曝光更柔和、饱和度往铜橙收(0.92)、
  叠真实月面色偏(权重0.5)。月盘平均亮度落中调，红→白过渡自然，不再一侧死黑。
- 影调微调：初版 dyn_gamma=0.42 压过头，月盘亮度太平、浅色泛滥显得绿松石带占1/3。
  诊断：梯度被"红核曝光目标"锁死，关键控制点是 target_srgb 而非 dyn_gamma。
  调 target_srgb 0.42→0.24 让红核回深铜红(sRGB lum~51)、dyn_gamma 0.42→0.7，
  远/近亮度比 2.8×→4.8×(真实3:1~5:1中段)，红核占盘面大部分、青绿白退回边缘窄带。
- displacement 确认不做：月食是满月、太阳正对月面(相位角≈0)，几乎无地形投影阴影。
- HDR gain map：复用 YabiVision(自有iOS app) 的 Apple ImageIO 写入管线(macOS兼容)，
  但 gain map 从真实 HDR/SDR 比值精确算(非 YabiVision 从SDR猜)。Python 导出 SDR base
  + 线性HDR TIFF(峰值~16, 含>1)，Swift CLI(tools/make_gainmap_hdr.swift)算 gain map
  (log2编码,headroom~5档)写带 gain map 的 HEIC。一条命令: scripts/make_hdr.sh。
  下 iPhone Photos 亮部(绿松石/白边)超亮。HDR 仅物理iPhone+HDR屏可见。

### 2026-05-31（续）— "青绿"是相对色的诊断

- 接入 SAO2010 实测太阳谱替换黑体（净效应绿松石带红蓝比 0.745→0.720，偏蓝略增，
  因实测红端也偏低，与简化高斯预测相反；影响肉眼不可辨，物理更严谨）。
- 文献 cross check：绿松石带真实只占月盘直径 3.5-5.5%(1-2 arcmin)，我的 20% 偏宽 4 倍，
  根因是把"趋白过渡区"算进了绿松石带（纯青绿带文献定义只是红蓝比 0.8-1.0 那一薄层）。
- **debug "为什么偏蓝白不是青绿"——判决性结论：没漏物理机制，"青绿"是相对色**：
  - 绝对色品：绿松石带 dx=-0.055 dy=-0.055（往蓝，非青绿 dy>0）
  - 去瑞利/加气溶胶(AOD0.1-0.8)/模拟多次散射回填——全都 dy<0，到不了青绿
  - 纯臭氧吸收太阳谱留下的本来就是偏蓝冷色
  - **相对红核**：dx=-0.259 dy=-0.144（巨大的冷色对比）。红核(0.526,0.416)暖橙红 vs
    带(0.267,0.272)冷蓝。人眼/相机对暖红视场色彩适应后，这条绝对偏蓝的带被**感知**为青绿。
  - 即：turquoise 是色彩对比/白平衡的相对观感，不是绝对青绿。模型物理是对的。
  - 文献佐证：红蓝比0.8-1.0、"肉眼几乎不可见"、"靠HDR才显出来"=淡偏蓝冷色，非饱和teal。

### 2026-05-31（续2）— 几何大纠错 + 蓝带构图

- **重大纠错：删除错误的"对侧-limb"公式**。之前为修一个不存在的 bug 引入的
  r_opp=R_umbra-(α·d-h) 是错的，它人为把绿松石带拉到 38-41' 造出"青绿月盘"假象。
  两个独立 agent + Mallama Table 3.1 确认：只有一套映射 r(h)=(R⊕+h)-α·d_moon，
  全程吻合<2%。月面每点只由一族同侧 limb 照亮，无"红核一 limb 绿松石另一 limb"分工。
  render_rt 合并双表为单一映射（之前双重计数）。文献作者更正为 Mallama（非 Robinson）。
- 真相：绿松石带(h12-18km)在角距 41.5-49.8'（本影边界 41.2' 外），极窄、淡。
- **蓝带构图（用户参考图厘清）**：摄影师拍的"蓝带"不是细环，是**月盘横跨本影边界**——
  半个月盘深入本影(红)、半个在本影边缘/半影(被臭氧染蓝)。月球在动，可停在任意 d。
  d=40' 时月盘横跨边界，左红右蓝，对上参考图。蓝半边经诊断是真臭氧蓝(角距48-55,红蓝比0.87-0.97)
  混趋白，非纯半影。render_textured 默认 d=40，输出 moon_bluefringe.png + HDR HEIC。
- 蓝带饱和度适度提升(saturation 1.15)是艺术处理(参考图本就来自"如何修图"教程)；
  物理真值的蓝偏淡，浓蓝有后期成分。

### 2026-05-31（续3）— double check 参考图：后期产物，我们物理对

- 用户拿一张 B站"月食蓝带"图(半盘饱和蓝)质疑我们的淡蓝。调研结论：那是**重度后期产物**。
  - 作者是一位风光后期教程UP主(匿名处理), 标题即"如何修图"。手法: HDR堆栈+HSL青色通道
    饱和度暴拉+局部蒙版强化, 把淡蓝/渐变区拉成近霓虹饱和蓝。"卫星映衬"假说证据为零。
  - 同类作者亲口"两张HDR合成强化绿松石蓝带"、"裸眼难注意, HDR实现被忽视的色彩"。
- **我们的物理被独立实测逐条印证**:
  - 红蓝比0.8-1.0: GOES-16卫星实测(MDPI RemoteSens 2024)证实, 我们最蓝0.87落区间内
  - 窄带1-2arcmin/月盘8%: 与Wang2024一致
  - 偏蓝紫淡色非饱和、肉眼几乎不可见: 与专业天文摄影(Shaw/Horálek/Dyer)原图一致
  - 真实蓝带=沿月缘一道浅蓝青绿细窄光带, 非半盘饱和蓝
- **结论**: 我们和参考图不一样, 不是物理漏了什么, 是参考图后期把淡蓝拉成饱和蓝。
  一路坚持第一性不为"凑好看的蓝"改物理, 被实测验证是对的。

### 2026-05-31（续4）— 蓝带"宽窄"的终极 deep dive：亮度悬崖

用户深究"蓝带是突变还是渐变、一小条还是半盘"。三层结论：

1. **颜色：是青(cyan/teal)，不是蓝也不是"红带紫"**。最蓝处(角距52')sRGB=(221,254,254)，
   G/B 都到顶、R 稍低，主波长475nm。"红蓝比"指标只比红蓝、漏了绿，**误导**。真实是青绿
   （臭氧吃600nm橙红，留蓝+绿=青）。
2. **文献1-2arcmin的定义**：Shu et al. 2024 (RemoteSens 16(22):4181，不是Wang) 用
   **R/B<1 阈值切片**全宽，不是FWHM/硬边界。我们最蓝0.87落在其实测0.8-1.0内，对得上。
3. **"蓝带窄"本质是亮度现象，不是颜色现象（关键发现）**：
   - 颜色(R/B)是平滑渐变跨~14arcmin，无突变——这个我们和文献一致(真实照片RGB剖面、
     含/不含臭氧模型形态都自洽)。
   - 但**亮度悬崖**：最蓝处(R/B最低)亮度也最低(仅月盘最亮的~4.5%)，旁边趋白区暴亮20倍。
     真实照片里亮区盖过暗蓝带，肉眼只见一丝≈1arcmin = 文献"细窄光带"。
   - **我们之前的渲染 tone map"为暗部曝光"把暗蓝带强行提亮，拉成占月盘29%的宽渐变——
     这是可视化误导，不是物理bug。** 用户的"应该是一小条"直觉正确。

**修复**：render_textured 默认改 expose_mode="faithful"（按月盘最亮处标定、dyn_gamma=1.0
不压动态范围），保留亮度悬崖→蓝带因暗被亮区盖过、自然收窄成细带，符合文献与真实照片。
代价：红核很暗（物理真实）。诊断图 diag_brightness_cliff.py 对比两种曝光。

### 2026-05-31（续5）— 对偶视角：从月球看地球 + 视频数据导出

- 新方向：月食时站在月面中心看地球——这是同一辐射传输的**对偶视角**。
  从月球看地球(角半径57')完全盖住太阳(16')=日全食，黑夜面地球+一圈折射点亮的大气环。
  **大气环两维度**：径向(内→外)=擦边高度0→30km=红→青绿→白(=日落剖面，复用 render_rt 物理)；
  方位(绕圈)=太阳可见量=亮度，D 决定哪侧亮(D=0全圈均匀→D大一侧亮→D>edge钻石环)。
- **对偶统一**：月面某点颜色 = 站那点看到的地球环颜色。月面绿松石带的青绿 = 从月面边缘看，
  地球环朝太阳那侧(亮侧)、高空(平流层)那一段。所以长焦该看**亮侧**(太阳露出那侧)，非暗侧。
- render_earth.py：黑夜面地球(Black Marble灯光) + 物理大气环(径向查透射谱LUT,方位亮度调制)，
  支持长焦小FOV看环细节。环真实厚度仅1.3%(看不见)，展示放大~8×。
- **视频目标**：360帧，月心距本影中心 D 0→60'，左月面右地球(画中画)。
- **photorealistic 决策**：核心原则"我们算物理颜色，渲染器负责好看"。先把物理/数据出完，
  引擎(Unreal等)以后定。export_video_data.py 导出引擎无关数据(outputs/video_data/)：
  月面色LUT、地球环色LUT、每帧几何、meta.json。月球可用 color+displacement 在UE photoreal，
  地球大气环需体积发光介质(UE体积材质/Niagara)，颜色由我们的LUT喂。

### 2026-05-31（续6）— 月食对偶视频 + 三panel独立 tone mapping

**方法**：左月球(随D移出本影) | 中地球全景(站月面看,夜面+大气环+太阳钻石环) | 右大气环长焦特写。
对偶: 月面颜色=站那点看到的地球大气环颜色。D=月心距本影中心 10'→60', 300帧60fps并行(8秒)。
SDR(H.265 8bit) + HDR(HDR10, BT.2020+PQ, Python里PQ编码绕过ffmpeg缺的zscale) + 16bit线性TIFF(后期)。

**关键技术决策 — 三个 panel 独立 tone mapping**（核心）:
- 每个panel(月球/地球全景/特写)用**各自独立**的 tone map, 时间上全局定死(不per-frame)。
- 为什么独立: 共用全局tone map会让地球被月球的亮度范围带着走→地球过亮。独立后各管各的。
- SDR每panel: `floor + (1-floor)·Reinhard(exp·Y)` — 暗部抬到floor保底可见, 亮部Reinhard软压。
- HDR每panel: `nits = black + exp·Y·white` — 暗部抬到black保底, 亮部**线性不封顶**(保亮度增量),
  各panel独立映射到nits后拼接再PQ。这样大气环太阳露出后越来越亮(不封顶), 地球夜面独立压暗。

**踩的坑(都已修)**:
- per-frame normalize: 月面每帧按自身最亮归一→亮度恒定丢演变。改全局固定曝光。
- 全食死黑/血月: 全局动态范围压缩(SDR gamma / HDR black floor), 血月暗部保底可见。
- 大气环厚度/曲率: 真实薄环(1.3%) + 小fov长焦放大→弧趋平(像ISS看地平线)。撑厚环是错的。
- 太阳方向: 月亮往右移出→太阳从地球**左缘**露出(物理一致)。月盘明暗交接(D=41)=太阳露出(钻石环)同步。
- 月面右边暗: limb darkening太强(z^0.5)→月球近朗伯体改z^0.15; 半影白点设正常月光(连续不暴跌)。
- **聚焦因子亮斑**: focusing_factor的1/r在本影中心发散, 加硬r_floor正则化→造边界清晰亮斑(随D移动)。
  视频关掉(use_focus=False)。绿松石带研究保留(它只乘亮度不碰色相, 不影响颜色结论)。
- HDR走独立PQ路径, SDR的修复(关聚焦/独立tonemap)最初没应用到HDR→HDR三问题重现。改HDR也per-panel。

## Lessons Learned

- **颜色归一化常数 k 必须从未衰减入射日光算一次、全程复用**。per-height 归一会抹平本影变暗的亮度阶梯，制造假象。这是物理正确性要求，不是美化。
- **折射几何只有一套正确映射** `r(h)=(R⊕+h)−α·d_moon`（贴轴），全程吻合 Mallama Table 3.1。
  曾误以为需要"对侧-limb 公式"分管绿松石带——那是**错的**，已删除。月面每个径向点只由一族
  同侧 limb 照亮，一个 ρ 对应一个 h。"高 h 落到 41' 边界外"不是 bug：r(h) 零点是反日轴，
  41.2' 是太阳圆盘本影边界，高 h 弱折射本就落半影内沿。教训：为"修 bug"加第二套公式前，
  先确认那真是 bug——它可能是物理事实被误读。
- **色相偏差先怀疑度量，再怀疑物理**。CIELAB hue 角会把视觉温和的偏蓝放大；黑体白点偏暖会进一步推蓝。判色相用 sRGB 红蓝比更诚实。绝不为凑漂亮色相去改物理。
- **聚焦因子方向**：低擦边高度光线会聚到近本影中心，使中心更亮（红核虽消光重却最亮）。公式漏 `/r` 项会反向。
- **臭氧截面用实测（Serdyuchenko 2014），不用窄高斯近似**。近似带形太窄、红端吸收不足，绿松石带会偏蓝紫。
- 数据文件 `data/raw/o3_serdyuchenko_2014.dat` 是 12MB，已入 git，是项目核心数据不要删。
- 验证哲学：每替换一个近似为真实数据，对比曲线确认没回归；每步都要有能直接看的标量/曲线。
- **渲染 banding 的根因是拓扑/坐标错误，不是分辨率**：非单调映射 argsort 压平 + 角距轴插值。加密 LUT 治标不治本。正解是分支感知反查（render_rt.py）：在单调分支内反查 a→h，在光滑 h→颜色取色。逐像素 brentq 求根是错误优化（慢且无精度收益，颜色对 h 光滑）。
- 月面纹理与物理食光颜色必须在**线性空间相乘**（reflectance × emission），再 tone-map+gamma。
- 旧 render.py 的 build_lut/lookup_xyz 已被 render_rt 取代；render_textured 现走 RT 着色。
- **绿松石带的"青绿/turquoise"是相对色，不是绝对色品上的青绿**。纯臭氧 Chappuis 吸收
  (吃600nm橙红)作用在太阳谱上，绝对色品是偏蓝(dy<0)。瑞利/气溶胶/多次散射都改不成青绿。
  人眼/相机对暖红月盘做色彩适应后，那条相对冷的带才被感知为青绿。物理模型是对的，
  不要为了"凑出绝对青绿"去加假机制。要画面青绿感→显式做色彩适应(艺术),不是改物理。
- 太阳谱(黑体 vs 实测)对绿松石带色相只差~3°，肉眼不可辨；敏感性低，但实测更严谨。
- **"蓝带窄"是亮度现象，不是颜色现象（最重要的教训）**。颜色(R/B)沿月盘是平滑渐变跨~14arcmin、
  无突变；但最蓝处亮度也最低(月盘最亮的~4.5%)，旁边趋白区暴亮20倍(亮度悬崖)。真实可见蓝带=
  "又蓝又够亮"的交集，被压成~1arcmin细带。我们长期在**颜色**维度纠结蓝带宽窄，忽略了
  **亮度悬崖**才是让它"看起来窄、有边界"的主因。
- **曝光/tone map 策略会决定性地误导对物理的判断**。"为暗部曝光"把暗蓝带提亮，使物理上窄的
  蓝带显成占月盘29%的宽渐变——看似物理问题，实为可视化问题。诊断现象前先问：是物理本身，
  还是显示策略(曝光/tone map/动态范围压缩)造成的？render_textured 有两种 expose_mode：
  faithful(默认,保留亮度悬崖,蓝带自然窄,物理真实) vs darks(提暗部,蓝带显宽,好看但误导)。
- **多次询问"参考图为什么和我们不一样"时，先查参考图的来源与后期**。用户的 B站参考图(半盘饱和蓝)
  是风光后期教程作者用 HDR堆栈+HSL青色通道暴拉 做出来的，不是物理真实。我们的淡蓝渐变窄带
  被 GOES-16卫星实测(R/B 0.8-1.0)+Wang/Shu(1-2arcmin)+专业天文原图逐条印证。
  教训：别被重后期的"网红图"当成 ground truth 去倒逼改物理。
- **沿途坚持第一性、不为"凑好看结果"改物理，多次被实测验证是对的**（偏蓝、带宽、青绿、蓝带窄）。
  每次"看着不对"的诱惑，正解都是回到物理诊断或检查度量/可视化，而非加假机制(气溶胶/调臭氧/硬染蓝)。
- **视频渲染: 多panel组合时每个panel要独立tone map**。共用全局tone map会让暗panel(地球)被亮panel(月球)带跑。独立后各管各的曝光/动态范围。这对SDR和HDR都适用。
- **HDR走独立路径, SDR的修复未必自动应用到HDR**。改完SDR务必单独验证HDR(它的tone map/PQ编码是另一条链)。用户看视频反馈的问题要分清是SDR还是HDR。
- **聚焦因子1/r在中心发散造亮斑**: 是真实会聚物理(中心更亮)+非物理的点源假设(真实太阳有张角,会聚到有限光斑)。硬设r_floor正则化→造边界清晰亮斑。视频关掉。它只乘亮度不碰色相,不影响绿松石带颜色结论。
- **诊断视频问题先从上传的视频抽帧, 不是单独渲的PNG**。我曾一直看单帧PNG调试, 但用户看的是视频, 两者可能不一致(残留旧帧/路径不同)。
- **月球近朗伯体, limb darkening很弱(z^0.15)**。用太阳那种强limb(z^0.5)会让满月不均匀("右边暗")。

### 2026-05-31（续7）— reassess 绿松石带(改bug后against文献)

视频轮改了几处会影响 render_rt 的东西, reassess 绿松石带物理是否还符合文献:
- **聚焦因子**: 视频关掉(build_video的MOON_T use_focus=False), 但 render_textured 默认仍 use_focus=True
  → 绿松石带渲染不受影响。且聚焦只乘亮度不碰色相, 不影响颜色结论。
- **半影白点改连续**(penumbra_Y 0.20→LUT边缘≈正常月光1.0): 影响半影区亮度。
- **结论: 绿松石带核心物理仍符合文献, 亮度悬崖更强(蓝带窄结论更成立)**:
  - 红蓝比 R/B=0.87-0.88(文献0.8-1.0) ✓ 不变
  - 最蓝在角距52', 绿松石带擦边高度19-25km ✓ 不变
  - L1闭环六项自查全过 ✓
  - 亮度悬崖: 最蓝处(角距52)Y=0.004 → 半影(58+)Y=1.0 = 250×悬崖(之前半影=0.20时~50×)。
    半影改成正常月光更亮→蓝带相对更暗→"蓝带窄是亮度现象"结论**更成立**, 非回归。
- 无回归。改bug(连续半影/视频关聚焦)对绿松石带物理结论无负面影响, 部分更稳。

### 2026-05-31（续8）— SDR tone map 三问题修复(用户截图反馈)

用户对 D=44.4 帧 SDR 提三个问题, 根因都是 **tone map 加得太平**(动态范围压缩过头, 曲线斜率太低):
1. 月球: 右(出本影/正常月光)vs 左(深本影/血月)该高对比, 但被压得差不多亮。
2. 地球: 夜面太亮, 和外面折射环几乎一样亮。
3. 大气特写: 从30角分到最后再没变过, **而且还有颜色(没饱和到白)**——太阳露出该 bloom 到白。

**修复**(`_panel_tonemap` 旧 gamma+对数肩部 → 分段曲线):
- 暗部(Y≤knee=1): gamma 提亮保层次, knee处=target。
- 亮部(Y>knee): **线性快速冲到1(白)**——去掉对数肩部, 高光能真正 clip。
- 这样既保 spatial 对比(月球右亮左暗), 又让高光饱和(钻石环/大气 bloom 到白)。
- 三panel参数: 月球 g=0.80 tgt=0.78(接近线性保大对比, 暗部仍有血月层次);
  地球全景 g=0.55 tgt=0.72; 大气特写 g=0.62 tgt=0.70。
- 地球夜面系数 `render_earth.py` 0.12→0.045(明显暗于折射环, 城市灯光仍可见)。

**验证(逐问题)**:
1. 月球 D=44 线性左0.022/右0.167=7.6×真实对比, 旧曲线压成显示3×, 新g=0.80显示5×, 目检右下出本影亮/左血月暗。
2. 地球夜面比折射环暗 4.6×, 全景帧目检夜面深蓝、环红边清晰。
3. 大气特写 clip到白随D: D=44=0%→D=50=3.3%→bloom出现; 亮部95% D=33→56: 0.71→0.98, 不再"再也不变"。

**HDR**: `_panel_to_nits` 用线性Y(不压gamma), 对比天然全保留(月球D=44暗11→亮105nits),
钻石环 max 515-1076 nits(HDR bloom 强项), 夜面1nit(暗但HDR屏可见)。HDR三问题本就更好。

两版重渲上传 yage.ai/share/eclipse/(SDR 268K, HDR 221K 10bit PQ BT.2020)。

### 2026-05-31（续9）— 发现：点源近似把绿松石带画偏蓝过头（圆盘ray tracing修正）

用户质疑：算绿松石带时太阳被当**点源**，没考虑它有 16' 角径（有限大小）。
查证属实——两个层面的近似：
1. **照度几何**：`r(h)=(R⊕+h)−α(h)·d_moon` 是**反日轴单光线**(点源)映射，每个月面点只被
   单一擦边高度的一条光线照亮。太阳圆盘的角度展布(±16')没进照度积分，靠 focusing_factor +
   r_floor 软下限近似糊。(辐射传输 `I=I_sun(λ)·T(λ,h)` 只含太阳**光谱**，不含**角度形状**。)
2. **环色不自洽**：render_earth 环把色相(擦边高度查谱)和亮度(几何 bright 函数)**解耦**，
   色相随 D 不变 → 环积分色恒定，与月心色对不上。

**圆盘 ray tracing 量化（折射感知，反日轴 1D，太阳圆盘弦长加权积分）**：

| 量 | 点源版(现状) | 圆盘ray tracing | 文献观测 |
|---|---|---|---|
| 最蓝 R/B | **0.41** | **0.97** | 0.8-1.0 |
| 亮度悬崖宽(Y0.05→0.5) | 18' | 18' | — |

**关键结论（反直觉，验证两轮）**：
- 太阳圆盘**不是**把绿松石带抹没，而是把颜色从**过饱和的 0.41 柔化到符合观测的 0.97**。
  即：**点源版把绿松石带画得偏蓝过头了**，圆盘版才匹配真实月食照片(淡青非浓青)。这是真 bug。
- **亮度悬崖几乎不变(18'→18')**——悬崖由折射几何 r(h) 陡度决定, 16'圆盘平滑相对悬崖尺度小。
  所以"蓝带窄是亮度现象"核心结论**不受影响, 仍成立**。
- 易错点：完整圆盘卷积(不考虑被地球挡)会错算成 R/B=0.97@均匀→颜色冲白; 必须用**折射感知**
  的 ray tracing(子点偏移ξ平移落点, 反查 a_signed(h)=Dp−ξ), 才得正确的月牙可见太阳贡献。

**下一步**：把折射感知圆盘 ray tracing 推广到全月面 2D(GPU/MPS)，一镐子替换
render_rt(月面研究图) + 视频月面panel + render_earth环(自洽：环也用同套RT，积分=月心色)。
单点物理已验证通过(上述数据)。

### 2026-05-31（续10）— 纠正续9：金标准暴力ray tracing定论(圆盘R/B=0.71@40', 非0.97)

**续9的 R/B=0.97 结论错了**(那个1D卷积漏focusing+几何处理不当, 巧合凑到0.97)。
用 workflow 三路独立验证, 写了金标准暴力 ray tracing `src/brute_ray_trace.py`:
撒20万条平行光线×257太阳圆盘子点, 按α(h)折射追踪落点, 落点分箱累加XYZ,
**focusing从落点密度自然涌现(不乘任何focusing公式)**。退化成点源(n_xi=1)精确复现
已知点源物理(R/B=0.413@52.6', 悬崖1.24')→ 方法可信, 当金标准。亲手跑过验证。

**真实物理(金标准, 三方向n_h/n_xi/bin全收敛)**:

| 量 | 点源近似 | 圆盘ray tracing(真实) |
|---|---|---|
| 最蓝 R/B | 0.413 | **0.710** |
| 最蓝位置 | 52.6' | **40.4'**(移到内沿) |
| 亮度悬崖宽(Y5%→50%) | 1.24' | **3.48'**(软边) |

**关键物理(回答用户两个原始质疑)**:
1. **太阳形状是真bug, 影响比想象大**: 太阳全宽32' > 绿松石带角宽~6' 的5倍。卷积核比信号宽,
   把窄青谷糊平。点源版把带画得**太浓(0.41)、太靠外(53')、边缘太锐(1.2')**;
   真实是**浅青(0.71)、靠内(40')、软边(3.5')**。最蓝移内沿因为只有靠内处高空白光份额
   还没反超、青味才残留最浓。
2. **18'悬崖是漏focusing的错误中间值**。真实悬崖点源1.2'/圆盘3.5', 不是18'。
   圆盘把锐边按~16'太阳角径模糊→3.5'软边, 这才是真实月食看到的(几角分软边非锐边)。

**待修定标点**(a_signed agent指出): 越缘白光(target>a_hi)用了 Y≈1 满直射日光, 对被地球
大半遮挡的圆盘点过亮; 圆盘窗口扫到57.7'以上需核对月面坐标→a_signed零点/符号定标。

**下一步**: 用金标准这套(brute_ray_trace思路)集成进render_textured渲静态对比图(点源vs圆盘),
目标值 R/B 0.71@40'/悬崖3.5'(不是点源0.41, 那在有太阳圆盘的真实世界不存在)。

### 2026-05-31（续11）— 圆盘物理集成进视频+静态图 + 方法论文档

**静态研究图**(用户确认"像摄影师拍的, 这版对了"): outputs/moon_disk_turquoise_final.png
- d=40'(圆盘版最蓝处40'过月盘中心; 点源版53'已废)。忠实物理: 浅青软边、偏左(朝血月侧)、不夸张。
- 金标准圆盘LUT × 月面反照率纹理 × limb × 动态范围压缩(dg0.55)+轻饱和(1.3)。
- 用户观察绿松石带"偏左不在中心"→正是圆盘版最蓝移内沿40'的体现, 物理对。

**视频集成**: build_video 月面panel 点源shade → 圆盘 `build_disk_lut`/`shade_disk_lut`(render_rt.py)。
- a→XYZ LUT 与d无关, 建一次(300k光线×257子点)全帧复用。圆盘天然无亮斑(正则化中心发散)。
- 月面tone map遵循同标准: 明暗交界帧(D38-46)暗侧≈0.09-0.12(达标≈0.1), 亮侧0.63-0.70足够亮。
- 视频月面保spatial对比(右亮左暗), 不强求显浅青(青色展示交给静态研究图)。三panel解耦tone map不变。
- SDR重渲上传(de52a8, 316K)。HDR本轮未动(上轮已好)。

**方法论文档**(PHYSICS_AND_PITFALLS 新增"二·五"): 统一为一个多维积分ray tracing。
回应用户理解(基本对): 三空间有限元(太阳圆盘/大气高度/月面)×三光谱(太阳/瑞利/臭氧)×时间。
**标明漏项**: 折射色散(α(h)当消色差, 最值得补)、单次散射、气溶胶。框架天然支持ablation归因。

### 2026-05-31（续12）— 修3个bug(用户末帧反馈): 月面阴影/折射色散/环方位variance

1. **月面末帧右缘移动阴影**(用户D=60帧): build_disk_lut在a>亮度峰值(≈本影边界57')后Y虚降
   (a=70只0.53)——暴力撒线在本影外/半影外落点密度不足的采样假象。物理上半影外=直射正常
   月光(最亮~1不衰减)。修: 峰值后XYZ全clamp到正常月光白。D=60右缘不再有移动阴影。
2. **折射色散**(用户指出真bug, 文档标的"最值得补漏项"): α(h)原消色差→按波长。
   加 Edlén n(λ): 蓝光(n-1大)折射更强→落点更靠外(像棱镜)。brute_trace分16波段各自色散落点。
   量化: 绿松石带青带宽 4.0→4.6'(+15%)、略蓝(R/B 0.708→0.698)、位置不变40.4'。二阶但真实,
   边缘更柔。做成 n_disp 开关(=1关色散, ablation用)。视频月面LUT默认带色散。
3. **环方位variance压平**(用户:太阳侧/背侧差不多亮, 疑白平衡): 诊断为tone map问题非白平衡
   (用户确认不管白平衡)。线性环方位比365x(太阳侧4.79/背侧0.013)被FULL_GAMMA0.55压成SDR 5.9x。
   g0.55→0.80接近线性, 方位比回12.7x(背太阳侧环暗下隐入/太阳侧亮)。SDR 8bit装不下365x, 12.7x够。

SDR重渲上传(9b6af1)。三修复目检确认(末帧月面均匀无阴影、环方位回来、夜面更暗)。

**Reassess(色散改动后无回归)**: 圆盘版(带色散)最蓝R/B=0.70@40.4'、青带38-42'(宽4.2')、
悬崖3.5'——核心结论(浅青/靠内/软边)全保持。退化点源(关色散)仍精确复现0.41@52.7'→方法没坏。
色散是二阶修正(青带4.0→4.2'温和展宽)。绿松石带物理结论稳。

### 2026-05-31（续13）— HDR月面动态范围 + Ablation study(6步图+翻页视频)

**HDR月面tone map**(用户: 本影血月太亮): MOON_HDR_BLACK 0.8→0.1。深本影血月从~1nit降到~0.3nit
(撑开暗端动态范围), 满月端WHITE=200不变(用户确认满月够亮)。HDR重渲上传(3e235d)。
(SDR环方位variance那轮的FULL_GAMMA 0.55→0.80不影响HDR——HDR线性nits天然保方位variance 634x。)

**Ablation study deliverable**(src/render_ablation.py + docs/ablation_plan.md):
- 物理开关验证: emergent_spectrum(rayleigh/ozone/gray/solar_mode) + brute_trace(n_disp/point_source)
  每步R/B符合预期: 步2黑白1.13→步3瑞利3.34(血月)→步4臭氧0.53(浓青高潮)→步5圆盘0.71(浅青反转)→步6真实0.70。
- 6步叙事张力排序(土+纹理合并/臭氧高潮/圆盘反转), 绿松石带横向2/3位置, 夸张增强(dg0.5 sat1.6)+版式注释。
- 翻页视频 ablation_flip.mp4(20s slideleft, 7张含真实版收尾)上传yage。
- 文章提纲在 ablation_plan.md(反直觉钩子: 臭氧吸收为何是窄带/为何全食没有/为何真实没那么浓)。文章待写。

### 2026-05-31（续14）— 2×2布局 + ablation改16bit线性TIFF(全局定标)

**视频2×2**(用户: 1×3手机不好看): 第四格=绿松石带光谱成因(瑞利+臭氧谱+透射窗口, 随D动态,
透射窗口擦边高度随月心角距移动)。render_spectrum_panel(matplotlib)。_assemble/_render_hdr_frame改2×2。
SDR(a20c58)+HDR(1ea646, PQ)重渲上传, 1086×1086方形。

**Ablation改为只出图(用户用剪辑软件做视频)**:
- 输出16bit线性TIFF(非夸张增强): HDR nits tone map(月面panel同套 black0.1/white200)。
- **全局定标**(用户: 几张图tone mapping要可比+之前太暗): 6张共用 240nit→65535满量程,
  最亮满月撑到98%(之前1000nit定标只占20%故暗)。globally consistent, linear, 跨图可比。
- **绿松石带normalize到2/3**(用户: 加太阳圆盘后青带移位会让观众困惑): 每步单独调D让青带落2/3。
  步1-4(点源)d=47.4, 步5-6(圆盘)d=35.2。无青带步用步4 d对齐, 月盘几何位置一致。
- **步2本影露极暗纹理**(用户debate: 全黑还是留纹理): 本影内不纯黑(×0.04), 留月面纹理作连续锚点
  贯穿6步(月亮还在、只是没被照亮), 也符合真实(地球反照/星光)。
- 交付 outputs/ablation/step_*.tif (6张)。

### 2026-05-31（续15）— 修圆盘LUT banding(分箱噪声) + 清理

**banding**(用户: 高分辨率ablation step5/6可见banding, LUT artifact): 根因是 brute_trace 分箱的
**蒙特卡洛统计噪声**(撒线落点分箱, 相邻bin计数涨落→a→XYZ曲线不平滑, 绿松石带区相邻bin ΔY/Y达3-7%)。
不同于render_rt早期治的拓扑banding。修: build_disk_lut/_build_lut_disk 对a→XYZ各通道高斯平滑
(sigma=2bins≈0.16', 远小于绿松石带4'尺度)。粗糙度(二阶差)降98%, 物理保真(最蓝R/B 0.699→0.701不变)。
依赖 scipy.ndimage.gaussian_filter1d(venv已装1.17.1)。重渲step5/6 TIFF。

**灯光提亮**(用户提过想只提亮城市灯光不提亮整个夜面): 试了暗部抬升曲线(_panel_to_nits_darklift),
但它把整个夜面都抬亮、达不到"只亮灯", 用户决定跳过。已撤销, HDR地球panel恢复原线性映射。

清理: trash tmp下本轮调试预览图。video_frames帧目录(中间产物)保留(gitignore不进库)。

### 2026-06-01（续16）— 带宽 reassess vs 文献(发现缺sun-normalization口径bug)

用户reassess: 用文献Shu2024**完全相同的口径**重测绿松石带带宽, 看圆盘版是否还对得上(用户印象
点源版对得上文献, 加太阳盘后somehow对不上了)。workflow三路并行验证(measure_ribbon.py)。

**文献Shu2024精确定义**(agent查RemoteSens 16(22):4181原文): R/B=B4(654nm)/B2(491nm)辐射比值图,
阈值R/B<1, 量penumbra-umbra**边界ribbon径向全宽**, 报 **120-190 km**(≈1.1-1.7', 月面112km/'),
in-band R/B 0.8-1.0。GF-4卫星500m/px真实2D月盘, ribbon受月海钛含量耦合(非纯大气)。
注: 文档之前记的"1-2 arcmin"是我们自己从120-190km换算的, 非文献原文单位。

**关键发现——缺 sun-normalization(口径bug)**: 之前算R/B<1区间巨宽(48-72'/2700km)的根因是
**太阳谱本身654/491比值=0.737<1**(太阳在491nm本就比654nm亮)。raw R/B永远从teal回不到1→区间无界。
文献R/B是**定标反射率比(中性白=1)**, 等价除掉太阳谱斜率。除以0.737归一后白阳光→R/B=1,
teal凹陷变成两侧被R/B=1夹住的**有界ribbon**。这是真口径错误, 之前一直没做。

**修正后带宽(文献口径)**:
| 度量 | 点源版 | 圆盘版 | 文献 |
|---|---|---|---|
| 连续R/B<1全宽 | 741km(6.6') | 3886km(34.7') | 120-190km |
| 边界梯度宽(10-90%) | 437km(3.9') | 364km(3.25') | (最贴文献口径) |
| in-band R/B | 0.86-1.0 | — | **0.8-1.0 ✓吻合** |

**结论(纠正/确认用户印象)**:
1. **点源版确实更接近文献**(741 vs 3886km)——用户印象对。加太阳圆盘后以"连续R/B<1全宽"度量
   **变宽3.5倍**(10'→35'), 元凶是32'太阳圆盘核卷积(对照实验: 关色散宽度几乎不变, 是圆盘几何)。
   圆盘把最蓝点外推53→64', 最低R/B从0.63抬到0.72(青和邻白混合→变宽变浅)。
2. **两版都比文献宽2-6倍**, 根因是**口径不完全一致(非bug)**: 文献量2D真实月盘边界的**径向梯度窄带**
   (一条边界线陡度), 我们1D轴对称模型量**整个内禀蓝区宽度**——不是同一把尺。
   用最贴近的边界梯度宽度, 圆盘364km最接近但仍2-3倍。
3. **in-band R/B 0.86-1.0 与文献0.8-1.0吻合很好**——颜色对得上, 偏差只在绝对宽度。
4. 残差来源: 缺有限太阳盘的半影压窄效应(1D盘源又过度washout)、缺月海钛耦合、平滑大气模型缺真实
   臭氧层陡边界、文献可能叠加亮度/信噪阈值切ribbon两翼。

代码 src/measure_ribbon.py(sun-norm的R/B径向剖面+ribbon带+2D R/B月盘图)。亲手跑过验证741km/6.61'。

### 2026-06-01（续17）— 光度曲线尾巴bug修 + 曝光档数cross-check + sun-norm范围澄清

**曲线尾巴bug**(用户眼尖发现: 曲线69'最大、70'急降不合理): a_signed折射光落点物理上限=57.7'
(高擦边高度h→80km落点角距极限), 超过它无折射光线能到, brute_trace统计网格延到72'那段是
**采样不足artifact**(counts从12818掉到3772, Y假性下降)。修: 峰值(56.9')后clamp成持平正常月光。
**未污染档数**(基准本就取峰值)。修正曲线 outputs/photometric_profile.png。

**sun-normalization范围澄清**(用户问): sun-norm只为对齐卫星定标(把太阳谱归一成等能量/反射率比),
**只对复现文献R/B带宽测量有意义**。模拟人眼实际效果(视频/研究图)**不该做**——已带真实太阳谱SAO2010,
做了反而篡改颜色。确认: 渲染管线(build_video/render_rt/render_textured/render_ablation/brute_trace)
**无任何sun-norm**, 只活在measure_ribbon.py。两者隔离, 现状正确。

**曝光档数cross-check**(agent调研)——⚠**纠正之前被巧合误导的结论**:

口径核对(关键): 我们模型的亮端**就是满月直射量级**——h=80km最薄大气透射Y=193.78 ≈ 未衰减直射
日光Y=193.80(差0.00档, 高空大气近透明)。我之前说"亮端非满月"是错的。11.3档那个数的0档基准
其实是**出本影聚焦峰**(Y=412, 比满月直射194亮2倍, 因focusing局部增亮), 不是满月直射。

**用满月直射(Y=194)作0档正确比较**:
| 区域 | 我们(vs满月直射) | 真实 |
|---|---|---|
| 本影最中心 | **−9.9档** | Keen深食测光 **−16.5档**(−0.3mag vs满月−12.7) |
| 古铜区 | −6.6档 | — |

- **真问题在暗端**: 我们本影中心−9.9档 vs 真实深食−16.5档, **偏亮~6.6档**。即我们模型本影中心不够暗。
- 之前写"11.3档差0.3档验证通过"是**被巧合误导**: robpettengill 11.6档的"全食起始"恰好和我们聚焦峰
  基准凑近, 但物理上不是同一对比。真实深食本影中心比我们暗得多。
- **暗端偏亮的根因(推测)**: 单次散射近似 + 圆盘ray tracing送太多折射光进本影中心; 真实深食有
  多次散射损耗、气溶胶、臭氧对红光的额外吸收让本影中心极暗(Danjon L=0-1.5)。这也呼应带宽偏宽——
  本影/红核偏亮→亮度悬崖对比不够极端→绿松石带相对不够窄。
- 注: 全食深浅(Danjon L)主因是**月球穿本影的深度(几何对中)**和**地球气溶胶**(火山后极暗),
  地月距离次要。AstroBackyard三档(轻/典/深)是不同全食事件非同一次。
来源: robpettengill/AstroBackyard/mreclipse曝光表, Keen测光(physics.SE 25749), Danjon(eclipse.gsfc.nasa.gov/OH/Danjon.html)。

### 2026-06-01（续18）— 暗端偏亮深究: 核实agent误解 + 定位真问题(本影中心擦边高度偏高)

agent调研论断我们"把散射当主光源、机制选错"——**亲手核实后这是agent误解**:
- 我们模型是 I=I_sun·T(λ,h) = **折射太阳光束 × 斜程Beer-Lambert透射**, 不是散射当光源。
  与agent引的García Muñoz&Pallé 2011/arXiv2112.08966说的"折射主导本影"机制**一致**, 没选错。
- 消光物理基础**正确**: 垂直瑞利τ@550=0.092(文献0.097✓), σ_ray@550=4.5e-27(文献4.3e-27✓),
  海平面数密度2.55e19✓, h=2km切向air mass=60×(斜程长路径对)。

**真问题定位**(本影中心偏亮~7等的根因): 不是物理基础错, 是**几何映射让本影中心对应的擦边高度偏高**。
- a_signed=0(本影几何中心)在我们映射 r(h)=(R⊕+h)−α(h)·d_moon 下对应 h≈1.6km。
- h=1.6-2km 红光衰减只~3.8等, 但arXiv真实本影中心disk-resolved红衰减**11等**(蓝22等)。
- 即真实本影中心由**更深的擦边光(h→0甚至擦地)**照亮(air mass更大、衰减11+等), 我们对应到了偏高
  的擦边高度→衰减不够→本影中心不够暗→偏亮~7等。这是消光深度不足, 非散射机制错。

**agent有用的部分**:
- 口径: 真实报的"亮全食−3~−4等"多是totality边缘/浅食, 非穿正中心。理论穿正中心+1.4等(很暗)。
  我们穿正中心−6等 vs 理论+1.4, 差7.4等(=上面的擦边高度问题)。
- 气溶胶解释真实食之间L0-4约5等差异(Keen: 每0.025 AOD≈1等), 不解释我们相对理论的系统偏亮。
- AstroBackyard三档是**同一次食的不同相位**(penumbral/partial/totality), 纯经验曝光、不提物理。
- 参考: arXiv2112.08966(中心+1.4/onset−3.5), DLR2025渲染(散射进本影<<折射), García Muñoz2011。

**待讨论**: 要不要修几何映射让本影中心对应更深擦边高度(更暗)。这影响暗端但**不影响绿松石带颜色/
位置结论**(那在本影边缘h~15km)。修了会让亮度悬崖对比更极端、更接近真实深食。先讨论再动。

### 2026-06-01（续19）— 暗端偏亮根因确认(解析权重非ray tracing) + opportunity sizing

**用户洞察(命中要害)**: 我们偏离了项目第一性原理——用了 a_signed/弦长权重/focusing 这些**解析近似**
代替老实 ray tracing。纯 ray tracing 里"相对权重"不该存在: 撒光线, 落哪加哪, 落点疏密(能量密度)
自动给出亮度, 不需要任何弦长权重/focusing 公式(那些本是 ray tracing 的自然产物, 我们当公式算→算偏)。

**Opportunity sizing(真·正向ray tracing, 撒20万光线×400太阳子点, 无弦长权重/focusing)**:
| 方法 | 本影中心(a=2)亮度 |
|---|---|
| 旧解析版(brute_trace弦长权重无focusing) | −7.7档 |
| **真ray tracing(撒光线落点自然分箱)** | **−11.0档** |
| 真实穿正中心(arXiv) | −14~−19档 |
- **老实ray tracing让本影中心暗3.3档**(−7.7→−11), 方向对、解决大部分偏亮。
  原因: 落点密度(能量/面积)自然给亮度, 解析弦长权重高估了本影中心亮度。
- 剩余3-8档差距: 落点几何仍用解析α(h)非真追踪折射弯曲; 单次散射/缺气溶胶; 能量密度归一细节。
- **判断**: 真ray tracing值得做(能解决大部分), 要完全对上还需把折射几何也做成真追踪。

**Plan(我的prioritization)**: 暗端真ray tracing修正**不影响绿松石带颜色/位置结论**(那在本影边缘
h~15km, 已验证吻合文献), 是polish。优先级排在**写文章之后**——文章是当前主线deliverable。
暗端修正作为后续physics polish。记入ablation_plan六节。

### 2026-06-01（续20）— 真ray tracing性能诊断(用户观察CPU/GPU占用低) + 轨迹合成图

**性能问题**(用户观察: CPU/GPU占用率mediocre, 但月食ray tracing是embarrassingly parallel该吃满):
诊断 workflow 生成的 src/raytrace_eclipse.py, 确认是真问题:
- 折射数值积分**逐高度串行for循环**(每个h一次ODE积分), 没矢量化。
- 落点散射用 **np.add.at**(出名的慢, Python级逐元素, 单线程)。
- 纯numpy CPU, **没用MPS/GPU**。
- 所以CPU没吃满核、GPU完全没用。这违背thesis的"GPU暴力跑光线追踪"。
**下一步**: 等synthesize确认物理对(本影中心能否-11→-16档)后, 做GPU/矢量化优化版。
用户的"CPU/GPU双跑结果一致=正确性验证"正好用GPU版vs这个CPU版互验。

### 2026-06-01（续21）— 真ray tracing成功: 本影中心-13.1档(逼近真实), thesis硬证据

workflow完成, 三路设计+集成(src/refraction_trace.py真折射RK4积分 + curved_path.py弯曲路径消光
+ forward_ray_trace.py撒线 + raytrace_eclipse.py集成)。**待亲手验证(不盲信agent)**。

**真ray tracing本影中心档数演进**(thesis的硬证据链):
| 版本 | 本影中心 | 说明 |
|---|---|---|
| 解析版(a_signed+弦长权重+focusing公式) | −7.7档 | artificial量 |
| sizing(撒光线但落点仍解析α) | −11档 | 部分涌现 |
| **真ray tracing(零artificial, 真折射+撒线)** | **−13.1档** | 全涌现 |
| 真实穿正中心(清晰天空) | −14~−19档 | 目标 |
绿松石带R/B=0.97(保住, 颜色结论没破坏)。

**关键诊断(完美印证thesis)**: 本影中心单条h≈2.7km光线单程透射仅−7.9档, 但binned面亮度−13.1档——
**比单条暗5.2档是几何散焦/通量稀释自然涌现的**(深擦边光摊薄到整月面, 最深光线α过大越过反日轴
落到r=−687km, 轴上只剩薄薄一圈h≈2.7km的光)。**focusing/散焦完全是撒光线涌现, 零解析公式**。

**纠正之前判断**: 我以为偏亮是"消光不够深", 但agent量化弯曲vs直线消光只差8%(h=0红4.44→4.79mag),
**消光不是主因**, 几何散焦才是。剩余1~6档(−13.1 vs −14~−19)是**真实气溶胶/云**(挡h<5km最贴轴光),
单次散射之外的真实大气调制, **非数值/归一化bug**。−13.1是清晰天空下界锚点, 真实随云量在−14~−19浮动。

**真折射的关键发现**: 解析α把70'钉在impact h=0是错的——真追踪显示h=0/1km光线弯曲后切点压到地表下、
撞地blocked、根本到不了月面。真正擦地的是impact-h≈1.72km α=63.5'。中高空真α比解析大(h=12km真21'
vs解析15.7')→向中心弯更多。

**意义**: 从−7.7(解析近似)→−13.1(真ray tracing)逼近真实, 剩余是真实物理非bug。
这是thesis"暴力第一性原理让focusing自然涌现、比解析近似更对"的最硬证据。
待: 亲手跑raytrace_eclipse.py验证 + 性能优化(GPU, 现版np.add.at慢)。

### 2026-06-01（续22）— 七步收尾(真ray tracing接入产品): 1-5步完成

用户七步计划: 落盘→test→review重构→性能→更新视频→更新ablation→polish艺术图。前5步:

1. **落盘**(c055f92): 本影中心-13.0档亲手验证。
2. **test**(6f1a8d6): 9个物理不变量测试(折射/消光/集成), pytest.ini注册slow marker。
3. **review+重构**(aacdc5d): (n-1)三处不一致(2.7/2.78e-4)统一到cross_sections.dry_air_n_minus_1
   单一来源; 删forward_ray_trace.py(dead code); ang_sun_rad→geometry/_M_XYZ2RGB→color共用;
   B6 blocked逐节点掩码修静默偏差。9 test全过。
4. **性能**(457d690): 折射RK4矢量化trace_rays_batch(锁步并行, **80×**, α逐位一致CPU双跑验证);
   np.add.at→np.bincount。完整ray tracing分钟级→**4.1s**, test 6min→1.8min。解决CPU占用低。
5. **视频接真ray tracing**(本轮): build_lut_from_raytrace建a→XYZ LUT(本影-13档真实暗, 出本影
   clamp真满月直射, 全帧复用)替换旧圆盘LUT(偏亮-7.7)。MOON_GAMMA 0.80→0.45(强暗部提亮让-13档
   暗本影的古铜血月可见有层次)。深食D=20现真实古铜色血月有月海层次, 明暗交界自然。
   SDR(51215e)+HDR(5db728)重渲上传。整个项目物理统一(视频/ablation/测光同一套真ray tracing)。

待: 6.更新ablation 7.polish艺术图。

### 2026-06-01（续23）— 半影直射光(消除光度曲线假跃变, 第一性原理涌现)

用户重画光度曲线时发现出本影处有跃变, 追问是不是bug(明确不要打补丁强行平滑)。
**诊断(诚实)**: 不是bug, 是两件事叠加——(1)折射ray tracing只算擦limb折射光(到-5.6档@58'),
半影区太阳探出地球缘的**直射光渐入**未建模; (2)我画图硬clamp满月1.0造假跃变。
用户决定: 老实ray trace直射光(物理), 不打补丁。这是该补的一块真物理。

**实现(零artificial, 同一套撒线涌现)**: 撒线impact b范围扩到 [R_e, R_e+h_direct_max=2500km].
b>R_e+h_max 在大气外=直射光: α=0(不弯)、满谱不衰减(满月白)、永不blocked。A_ring相应扩大,
等通量。半影区直射光渐入自然涌现。
**几何**: 本影边界41', 半影区41-73'(太阳从全挡到全露), 73'=满月。

**光度曲线(重画, 跳变消失)**: 本影中心-12.9档(古铜血月)→缓升→41'亮度悬崖(绿松石带)→
半影区直射光渐入**平滑爬升**→73'满月0档。两机制(折射光本影内+直射光半影区)平滑衔接, 零打补丁。
总动态范围12.9档。outputs/photometric_profile.png。

test加 test_direct_light_reaches_full_moon(出本影到满月>0.7+半影相邻档数<2平滑无跃变), 10 test全过。
下游(视频/ablation/艺术图)用含直射光LUT重算: 出本影满月不再硬clamp, 物理真实。

**轨迹合成图**(临时分享用, 用户明确是艺术图非测光精确): outputs/eclipse_composite.tif
- 斜25°(用户要30°/调研20-30°自然)、食甚铜红居中、星空(subtle带色点星)、本影段密满月端疏。
- HDR nits tone map(不夸张增强, 用户反馈之前那套怪)。月海纹理保留。
- **导出16bit线性TIFF撑满65536**(全局定标峰值→65535, 用户要求像ablation, 后期可改)。
- 待调: 本影段月盘还叠太密(糊成一条), 要拉开。参考: APOD Tezel食甚居中两侧对称、
  Clark星空直线序列、铜红提亮到可读月海可辨+偏食边缘带turquoise冷边。

**本影内缓升4-5档的物理**(用户直觉确认): 本影中心→边缘光度缓升, 因同一帧内月盘不同点对应不同
擦边高度——中心=阳光擦最低最厚大气(消光最狠/最暗/最红), 边缘=擦高层薄大气(消光弱/较亮/趋青)。
擦边高度0→30km, 大气光学厚度指数降→透射光指数升=缓升4-5档(纯折射,太阳仍全被挡)。
到绿松石带(~38-44', 本影边缘/半影内沿)亮度悬崖陡升=进半影、太阳从地球缘探头直射光漏入。
"缓升(折射) vs 悬崖(半影直射)"正是曲线log图本影内平缓斜坡、41'变陡的形状。

### 2026-06-09 — 外部三路 critique + 两个真 bug 修复 + 勘误

对项目做了三路并行对抗审查（代码-物理一致性 / 第一性+文献 referee / 发布就绪度），
报告存于内部工作区（code_audit / physics_review / publication_review / SYNTHESIS 四份）。核心结论（臭氧成因、浅青窄带、"蓝带窄是亮度现象"、
"turquoise 是相对色"）全部经受住审查；问题集中在暗端亮度、R/B 口径和文档漂移。

**修复1（真 bug）：弯曲消光把 impact parameter 当切点高度用。**
raytrace_eclipse 把 trace_rays_batch 返回的真实切点 z_tan 丢弃，拿 impact-h 喂
tau_curved（其输入语义是切点高度）。折射把切点压得比 impact-h 低（h=2.7km→z_tan≈1.1km），
深擦边消光被低估。修后 A/B（同 seed 同参）：本影中心 −12.80→−13.48 档（暗 0.68 档），
绿松石带不动（min R/B 0.692→0.699@41.0'）。这意味着续21"剩余差距全是气溶胶/云"的归因
其中 ~0.7 档其实是这个口径错误——解析量(impact-h)混进零-artificial 管线就会偏亮，
正好印证本项目 thesis。

**修复2（采样截断第三次复发）：直射光撒线范围不够。**
h_direct_max 默认 2500/3000 只把直射光干净覆盖到 ~63-68'，LUT 取到 73'，外缘满月端
假性掉到 0.88 且带非物理负斜率（photometric_profile.png 上 70' 后的下弯就是它）。
修：默认 5500（需 ≥ d·tan(a_hi+16')−R⊕ ≈3600），73' 处实测回到 0.995。
test_direct_light_reaches_full_moon 阈值 0.7→0.95（旧阈值拦不住）。10/10 test 过。
教训：撒线覆盖域要从"消费端最大角距+太阳半径"反推，这类采样截断假衰减已是第三次
（续12 末帧阴影、续17 曲线尾巴、本次直射光），应当成 checklist 固定下来。

**勘误1：续21 "绿松石带 R/B=0.97（保住）"是错误记录。**
两个独立 agent + 本机 A/B 复跑一致：当前真 ray tracing 最蓝 raw R/B≈0.70@41'，与圆盘
金标准 0.698@40.4' 相符，与 test 断言 <0.85 相符。0.97 在续9 刚被判"巧合错误"、续21 又
被当"颜色保住"的证据，同一数字双重身份没人对账。正确叙事：圆盘版与真 RT 颜色互相一致 ~0.70。

**勘误2：与 Shu 2024 的 R/B 比较口径。**
Shu 原文 R/B 是 B4(654)/B2(491) radiance 直除——含太阳谱斜率(654/491≈0.737)与月面反照率
红坡(≈1.3-1.4，月海/高地随 Ti ±15%)，两因子近似相消，不是续16 理解的"定标反射率比"。
即 sun-normalization 数值上歪打正着、物理理由错，且漏了月面反照率系统项(与文献 in-band
全宽同量级)。R/B 现存三种互不可换算口径（raw sRGB / sun-norm 654-491 / L0 gamma 均值比），
待统一为 654/491 radiance 口径并显式分解三因子（任务 D1b）。

**Open question（暗端 6 档，任务 D1a）：**
Mallama 原文核实：其模型 clear-sky、无气溶胶无云无臭氧，本影中心 disk-integrated
+1.4 mag（≈−18.7 档）、disk-resolved V −20.5 档，且与正常月食观测吻合。我们（修后）
−13.5 档。两个同样 clear-sky 的模型差 5-7 档，"剩余差距=真实气溶胶/云"不成立（Mallama
也没建模它们）。分歧定位候选：散焦量（我们涌现 5.2 档 vs Mallama Appendix A 1/1000≈10 档）。
另"真实 −14~−19 档"系混口径拼接（R 波段点值+integrated V），诚实锚点 −18.7~−20.5 档。
注：复跑显示本影内部 a=20-35' 整体比 Mallama 同位置亮 4-8 mag，系统性。

**文档换代（同步完成）**：P&P 坑4 按续2 真相重写（旧版把已删除的对侧-limb 公式教成解法）；
漏项清单更新（色散已做✓、新列 limb darkening/臭氧柱变化）；Robinson→Mallama、
Vollmer & Gedzelman 署名更正（P&P/SCIENCE_REVIEW）；README 状态/结论/主图/复现命令换代
（d=47 点源主图→d=40 圆盘版、"~4.5%"→"−10 档"、删 MPS 声称）；KTD 增"权威物理管线"条目。

**下游 artifact 注意**：视频/ablation step6/photometric_profile 均为修复前 LUT 渲出，
数字已过期（满月端假衰减+中心偏亮 0.7 档），任务 D1c 统一重渲。
